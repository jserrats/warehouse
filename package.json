{
  "name": "warehouse",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-run": "tsc && node dist/src/index.js",
    "build": "tsc",
    "deploy": "npm run build && docker build . && git add . && git commit -m 'deploying changes' && git push"
  },
  "author": "jserrats.xyz",
  "license": "ISC",
  "dependencies": {
    "@influxdata/influxdb-client": "^1.33.2",
    "dotenv": "^16.4.5",
    "mqtt-assistant": "github:jserrats/mqtt-assistant"
  },
  "devDependencies": {
    "@types/node": "^20.14.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.5"
  }
}
